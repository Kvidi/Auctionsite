@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Personuppgifter";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}
<!-- 🔙 Back Button Row -->
<div class="col-md-6 mb-3 text-start">
    <a asp-page="/Account/Manage/SettingsList" class="btn btn-custom-RoseWineBase">
        <i class="fa fa-arrow-left me-1"></i> Back
    </a>
</div>

<div class="row justify-content-center">
    <h3 class="form-title-Nav-Pages text-center">@ViewData["Title"]</h3> <!-- Centered and styled title -->
    <div class="col-md-6 form-container-Nav-Pages text-center">
        <!-- Styled container -->
                <p>Ditt konto innehåller personuppgifter. På den här sidan kan du ladda ner eller radera dessa uppgifter.</p>
        <p>
            <strong>Om du raderar dessa uppgifter kommer ditt konto att tas bort permanent, och kan inte återställas.</strong>
        </p>
        <div class="d-flex justify-content-center gap-3">
            <!-- Adjusted spacing between buttons -->
            <form id="download-data" asp-page="DownloadPersonalData" method="post" class="m-0">
                <button class="btn btn-primary" type="submit" style="background-color: #0056b3;">Ladda ner</button>
            </form>
            <a id="delete" asp-page="DeletePersonalData" class="btn btn-danger">Radera</a>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
